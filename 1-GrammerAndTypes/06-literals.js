// JS defines two types of values:
// 1: fixed values are called literals
// 2: varibles values are called variables

// Literals include: numbers, booleans, objects, and strings

let days = [ 'monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());

console.log(days.length);

let tired = true;

let soup = {
    a: 'chicken',
    b: 'tomato',
    c: ' beef and barley',
};

console.log(soup.b);

let car = 'Honda';

let decimal = 10.2;


